import React from "react";
import { constants } from "../constants/constants";

type Props = {};

const About = (props: Props) => {
  return <div className={`${constants["layout"]}`}>About</div>;
};

export default About;
